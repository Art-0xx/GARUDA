---
type: gtfobin
name: time
platform: Unix
functions: [shell]
tags: [gtfobin, unix, lotl]
---

# time

## shell

```bash
time /bin/sh
```
_Note that the shell might have its own builtin `time` implementation, which may behave differently than the binary, which is often located at `/usr/bin/time`._
**Contexts:** sudo, suid, unprivileged
